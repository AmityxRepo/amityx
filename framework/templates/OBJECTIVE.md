# Objective
<!-- Budget: 40 lines. "Goal" + "Acceptance criteria" form the Objective Digest pasted
     into EVERY agent spawn, so it must stay small: Goal ≤2 lines, ≤6 criteria one line
     each (≤16 lines total incl. headers). Self-contained; only the user's requirements
     live here; edits after Intake trigger a re-plan. -->

## Goal
<≤2 lines: what we're building, for whom, on what platform (web / mobile / both)>

## Acceptance criteria
<numbered, testable, ONE line each; the project ships when all are true — keep to 3–6.
More than 6 → split into milestones in PROGRESS.md, keep the digest to the top 6.>
1.
2.
3.

## Constraints
<required stack/services (default: git + Supabase + Vercel + Cloudflare, see
framework/INFRA.md), budget, deadline, compliance — or "none stated">

## Out of scope
<explicitly not building — protects against drift>
