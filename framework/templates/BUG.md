# B-###: <title>
<!-- Budget: 40 lines. Filed by the tester; Fix design added by the planner;
     closed by the tester after re-verification. -->
Severity: S1 blocker (security / data loss / core flow down) | S2 major | S3 minor | S4 cosmetic
Found by: TR-### while testing T-###
Status: open | fix-designed | fixed | verified-closed
<!-- Status ownership: tester sets `open` (on filing); planner sets `fix-designed` (when it
     fills Fix design below); developer sets `fixed` (in the fix task Result); tester sets
     `verified-closed` (after re-running the repro + regression). -->


## Repro steps
<numbered; must fail reliably from a clean start>

## Expected vs actual
Expected:
Actual:

## Evidence
<error output, failing request/response, screenshot path — something concrete>

## Fix design (planner fills)
<root-cause hypothesis, the fix, the regression test to add → becomes/updates task T-###>

## Fix verification (tester fills)
<repro re-run result + regression suite result, date>
