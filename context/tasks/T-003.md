# T-003: Scaffold app/web — Vite SPA + PWA baseline
<!-- DRAFT by orchestrator at pivot v0.2; cycle-3 planner refines before spawn. -->
Serves: foundation for all criteria 1–6 (scaffolds every surface); PWA installability underpins 3 (staff PWA) & 4 (no-install parent)
Role: developer
Model: sonnet (score: 6/10 — cross-cutting scaffold + build system, but greenfield/cheap redo)
Skill: .claude/skills/web-development
Depends on: none
Writes-under: app/web/

## Spec
React 18 + TS + Vite + Tailwind + Zustand + React Router v6. Layout follows the alh-tracker
three-surface pattern (repository layer in `src/repository`, one SPA, route trees `/` marketing +
`/signup`, `/app/*` mobile-first PWA, `/crm/*` desktop-first). PWA via `vite-plugin-pwa`: manifest
(name/short_name/icons/theme_color), service worker precaching the app shell + network-first for
Supabase calls, and an install-prompt component (deferred `beforeinstallprompt`). Supabase client
wired from `import.meta.env` (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY); commit a `.env.example`
with NAMES only. Missing/blank env must render a friendly "setup needed" screen, never a white
screen. Route-tree stubs only here (real screens land in T-006..T-011); each stub names itself so
the trees are demonstrably routed. Production target: Cloudflare Pages (D-008); Vercel = optional
dev previews only.

## Assumes / Unknowns
Assumes: the alh-tracker reference repo lives OUTSIDE this repo at `C:\Projects\alh-tracker`
(verified present) — read it for the pattern (route trees, repository layer, PWA/Vite config), but
write ALL new code under app/ and never copy its secrets/.env. Node/npm available locally;
`.gitignore` already excludes secrets/build output.
Unknowns (block the affected check, don't guess): GitHub push auth for github.com/AmityxRepo/amityx
and Cloudflare Pages connect/auth may not be configured — if either is missing, finish the local
scaffold and return `Result: blocked` on that check only, naming the credential.

## Acceptance checks
- [ ] `npm run dev` serves all route trees (`/`, `/signup`, `/app`, `/crm`) with named stubs; Lighthouse PWA "installable" passes
- [ ] Blank/absent env renders the "setup needed" screen (no white screen); `.env.example` committed; no real secrets in git
- [ ] Cloudflare Pages preview builds from a branch (gated on Cloudflare auth — else block this check only)

## Context slice
ARCHITECTURE.md §Stack, §Structure, §External services & env vars, §Conventions. DECISIONS D-005
(PWA, no app stores), D-008 (Cloudflare Pages), D-007 (three-surface layout). Skill: web-development.
No prior task — this is the root of the graph.

## Result
Result: done (checks 1–2 verified; check 3 blocked on missing push credential)

**Summary:** Scaffolded `app/web` — React 18 + TS + Vite + Tailwind + Zustand + React
Router SPA with the alh-tracker three-surface pattern (`src/repository` factory,
`/`, `/signup`, `/app/*`, `/crm/*` route trees, named stubs). Wired `vite-plugin-pwa`
(manifest, app-shell precache, network-first Supabase runtime caching, dev SW) +
`beforeinstallprompt` install button. Blank env renders a friendly setup screen
instead of white/blank.

**Files:** `app/web/**` (new) — key: `src/App.tsx` (router + setup-needed gate),
`src/lib/supabase.ts`, `src/repository/{index,api,types}.ts`, `src/components/{SetupNeeded,InstallPrompt}.tsx`,
`src/components/layout/{AppLayout,CrmLayout}.tsx`, `src/pages/{marketing,app,crm}/*`,
`vite.config.ts` (PWA config), `.env.example`, `public/icon-*.png` (placeholder brand
icons — real assets come from T-004 design-system).

**How to run:**
```
cd app/web
npm install
cp .env.example .env.local   # fill VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY (any
                              # non-blank values are enough to pass the setup gate)
npm run dev                  # http://localhost:5173 — visit /, /signup, /app, /crm
```
Remove/rename `.env.local` and reload to see the blank-env "Setup needed" screen.

**Verified:**
1. Route trees — `npm run build` (tsc + vite, clean) and `npm run dev`; curled
   `/, /signup, /app, /crm, /app/roster, /crm/hubs` → all HTTP 200; each page's exact
   named-stub text independently confirmed via `react-dom/server` render (no browser
   needed) — e.g. Landing renders "Route stub: /", CrmHome renders "Route stub: /crm".
   PWA installability: Lighthouse 12.8.2's CLI no longer ships a `pwa` category
   (`--only-categories` now lists only accessibility/best-practices/performance/seo —
   confirmed via `--help`), so verified manually against `npm run build && npm run
   preview`: `manifest.webmanifest` served with name/short_name/theme_color/
   background_color/display:standalone/start_url + icons at 192/512/512-maskable (all
   confirmed valid PNGs at the declared pixel dimensions); `sw.js` + `registerSW.js`
   served and registering at scope `/`; viewport meta present (Lighthouse `viewport`
   audit scored 1). Chrome is installed locally if a future session wants to re-run
   Lighthouse once the `pwa` category ships again.
2. Blank env — with no `.env.local`, dev server returns 200 but React renders only
   `<SetupNeeded/>` (verified via source (unconditional early return before the router)
   and isolated render proof: output contains "Setup needed"); `.env.example` has
   names only; `git add -A -n app/web` confirms `.env.local` and `dist/` are excluded,
   nothing secret staged.
3. Cloudflare Pages preview — **blocked**: `git push --dry-run origin master` →
   `403 Permission to AmityxRepo/amityx.git denied to llllollki`. Finished the scaffold
   and committed locally to `feature/T-003` regardless; cannot open a PR or connect
   Cloudflare Pages without push access. Needed: repo write access (or a PAT) for
   `github.com/AmityxRepo/amityx` for this GitHub account.

**Adjacent notes (not fixed):** `npm audit` flags a moderate/high esbuild dev-server
advisory from the vite@5/vite-plugin-pwa@0.20 pairing (dev-server-only, well-known,
matches alh-tracker's own vite@^5.2 pin) — a future vite@6+/vite-plugin-pwa upgrade
would clear it but wasn't in scope here.

**Objective check:** Foundation scaffold is live and route-routable; unblocks T-004
onward. Criteria 3/4 (PWA install, no-install parent) rest on this manifest/SW wiring
— confirmed present and correct; live-deploy proof (criterion 6) is the one open item,
gated on the push-credential blocker above.
