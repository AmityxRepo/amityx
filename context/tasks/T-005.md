# T-005: Tenancy schema + RLS migrations
<!-- DRAFT by orchestrator at pivot v0.2; cycle-3 planner refines before spawn. -->
Serves: criterion 3 (isolation) + underpins 1,2,4
Role: developer
Model: opus (score: 8/10 — multi-tenant security, blast radius)
Skill: .claude/skills/web-development (+ testing for RLS suite)
Depends on: T-003
Writes-under: supabase/migrations/, app/web/src/repository/

## Spec
Tables per ARCHITECTURE.md §Data model: hubs, hub_members, programs, class_sessions, children
(photo_consent), guardians, child_guardians, enrollments, booking_requests, announcements,
photo_moments, attendance, child_notes (visibility fields reserved), guardian_links (signed scoped
expiring tokens), crm_admins, crm_hub_profiles, crm_followups, crm_comm_log. RLS on every table:
tenant tables by hub_id via hub_members; crm_* by crm_admins; booking_requests INSERT-only from
public page (rate-limited); guardian_links = the ONLY parent read path. Platform support-access =
explicit grant + audit. Migration reviewed for data safety + rollback path stated.

## Acceptance checks
- [ ] Adversarial cross-tenant test suite (two hubs, two users) proves zero leakage incl. crm_*
- [ ] Seed script creates demo hub with multi-activity programs
