# T-011: Parent layer — announcements, photo moments, consent, media adapter
<!-- DRAFT by orchestrator at v0.3 ratification (D-009/D-011); cycle-3 planner refines before spawn. -->
Serves: criterion 4 (no-install parent layer) — the delight hook of the free layer
Role: developer
Model: opus (score: 7/10 — child-media privacy + token auth surface)
Skill: .claude/skills/web-development (+ testing: token/consent adversarial cases)
Depends on: T-005, T-007
Writes-under: app/web/src/ (parent views, media), supabase/

## Spec
Guardian access via signed scoped expiring links (guardian_links) sent by email/SMS-able URL —
NO account, NO install. Parent view (PWA-installable anyway): child's schedule, announcements,
photo moments. Staff capture: snap/upload → compress client-side (~200KB webp) → storage ADAPTER
(stage 1 Supabase Storage; stage 2 R2 per D-011 — interface designed now, flip = config).
Per-child photo_consent enforced at write AND read (no-consent child never appears in group-photo
distribution). 30-day rolling window purge job (free tier); all media URLs signed + expiring.
Announcements: aggregate view counts only.

## Acceptance checks
- [ ] Guardian link shows ONLY that guardian's children; expired/revoked token = clean denial
- [ ] No-consent child excluded from every parent-facing surface (adversarial test)
- [ ] Photo >30 days old purged on schedule; storage adapter swaps backends via env/config alone
