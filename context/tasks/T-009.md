# T-009: E2E suite + production deploy
<!-- DRAFT by orchestrator at pivot v0.2; cycle-3 planner refines before spawn. -->
Serves: criterion 6 (live at $0; guards hold) + regression net for criteria 1–5
Role: developer (deploy) + tester (suite authorship stays with tester per cycle)
Model: sonnet (score: 6/10 — deploy+harness dev work; the cycle's TESTER runs OPUS, see note)
Skill: .claude/skills/testing + .claude/skills/deployment
Depends on: T-006, T-007, T-008, T-010, T-011  <!-- planner refinement: added T-010/T-011 — this is the final integration+deploy task, sequenced LAST per founder order; the "regression net for 1–4" must cover booking (crit 2) + parent layer (crit 4). -->
Writes-under: e2e/, .github/ (Actions: keep-alive + CI), Cloudflare Pages build config, README

## Spec
Playwright suites (mirror the alh-tracker harness at `C:\Projects\alh-tracker` — read-only reference
outside this repo): demo/local harness + live smoke + authenticated live-journey runner. Journeys:
A owner self-serve signup → working hub session; B CRM sign-in → create hub → provision → hub-app
handoff; plus regression smoke for C public booking page → request lands in the /app Requests inbox
(T-010) and D guardian link → consent-scoped parent view (T-011). Deploy: run Supabase migrations →
build → **Cloudflare Pages PRODUCTION** (D-008) → post-deploy smoke on the live URL. Keep-alive: a
GitHub Actions cron issues a lightweight authenticated query against SUPABASE (a tiny RPC/select via
anon key — Supabase is what pauses, NOT Pages) on a <7-day interval (R-003). Verify $0: no paid-tier
toggles anywhere; document capacity ceilings (R-003) + the Supabase Pro upgrade trigger (first paying
hub) + the stage-2 R2 media flip (D-011) in the README.

## Acceptance checks
- [ ] Both journey paths green against production URL
- [ ] Out-of-scope guards asserted: no payment routes, no AI calls, PWA installable
- [ ] P.9 usability gates pass (D-012): 5-second test per screen, 3-tap audit on daily jobs
      (today's classes / check-in / post photo / read update), vocabulary grep vs banned-words
      list, icon-label + 44px/16px/AA sweep — violations filed as bugs (S2 if core flow confuses)
- [ ] Hallway test before public launch: one non-technical person completes owner setup + kiosk
      flow unaided; observed confusion filed as bugs

## Assumes / Unknowns
Assumes: T-006/T-007/T-008/T-010/T-011 are all merged (this is the FINAL integration + deploy task,
sequenced last). alh-tracker harness mirrored from `C:\Projects\alh-tracker` (read-only reference).
Unknowns (block the affected check, don't guess): Cloudflare Pages production connect/auth and GitHub
push auth for AmityxRepo/amityx are founder-provided; production Supabase env must be set.
ROUTING NOTE: the TESTER that verifies THIS cycle runs on OPUS — the cycle includes auth (T-006) and
child-media privacy (T-011), and MODEL_ROUTING forces the tester to the max task tier / opus for
security cycles. The sonnet score above is for the deploy + harness DEV work only.

## Context slice
ARCHITECTURE.md §Stack (Cloudflare Pages + keep-alive), §Conventions (Playwright, adversarial test),
§Data model (health-ping target). DECISIONS D-008 (Pages/prod), D-011 (media stages), D-012 (P.9
gates), D-014 (email-only). Research R-003 (capacity + keep-alive). Skills: testing + deployment.
Depends on T-006, T-007, T-008, T-010, T-011.

## Result (developer — DEPLOY half only; Playwright/P.9/hallway checks remain for tester)
Result: done (deploy + infra scope). Not yet done: e2e suite authorship, P.9 sweep, hallway test — tester's half.

**LIVE URL: https://amityx.pages.dev** (Cloudflare Pages project `amityx`, production branch `main`).

Summary: Added `public/_redirects` (SPA fallback, fixes T-010's flagged deep-route 404s) and
confirmed all deep routes (`/h/{slug}`, `/g/{token}`, `/app/*`, `/crm/*`, `/signup`, `/login`,
`/reset-password`, `/accept-invite`) return 200 live. Built + deployed to Cloudflare Pages via
`wrangler pages deploy`; verified the served bundle hash-matches the local build and contains the
real Supabase host `jjnzbayatcfkkoyorhes.supabase.co` (not a placeholder). Added
`.github/workflows/keep-alive.yml` (anon-key `hubs` select, twice-weekly cron) + `scripts/keep-alive.mjs`
mirroring T-011's purge-media pattern. Set 3 GitHub Actions repo secrets via the REST API
(libsodium sealed-box encryption, values never printed): `SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`,
`SUPABASE_SERVICE_ROLE_KEY` — unblocks both keep-alive.yml and T-011's purge-media.yml. Documented
live URL, redeploy command, and R-003/D-011 capacity ceilings + Pro/R2 upgrade triggers in
`app/web/README.md`. Guardrail grep: no Stripe/payment SDK dependency or import (one reserved
`stripe_customer_id` schema column, unused, no API calls); no R2/S3 usage in `src/` (D-011 stage 1
intact); Cloudflare Pages is the free tier, no paid toggle touched anywhere.

Files: `app/web/public/_redirects` (new), `.github/workflows/keep-alive.yml` (new),
`app/web/scripts/keep-alive.mjs` (new), `app/web/package.json` (+`keep-alive` script),
`app/web/README.md` (+Deploy section). Committed to `feature/T-009` (66c6ede), not pushed
(orchestrator handles push/PR per instructions).

How to run (tester): app is LIVE now, no local server needed — test directly against
https://amityx.pages.dev. To redeploy after further changes: `cd app/web && npm run build && npx
wrangler pages deploy dist --project-name amityx --branch main` (needs `CLOUDFLARE_ACCOUNT_ID` +
`CLOUDFLARE_API_TOKEN` env vars, values in `.env.local`, gitignored).

Verified: `npm run build` clean; `npm run test:rls` 98/98 regression green post-change; live curl
checks 200 on all route trees + PWA manifest/service-worker (200); GitHub secrets list API confirms
all 3 secrets created (values unreadable by design, as expected); keep-alive script tested locally
against live Supabase (status 200).

What the tester needs to know: (1) `.github/workflows/*.yml` won't actually fire on GitHub's
schedule until this branch is merged/pushed — orchestrator's job. (2) Workspace SMTP still unset
(STATE.md blocker #1, pre-existing, not in this task's scope) — auth emails send via Supabase's
default sender. (3) Playwright suite authorship, P.9 5-second/3-tap/vocab/44px sweep, and the
hallway test are explicitly the tester's half of this task, not built here.

Objective check: criterion 6 (live at $0, RLS-proven, guards hold) — live URL up, $0 confirmed (no
paid toggle, no payment SDK), RLS proof carried over from T-005/T-010/T-011 (98/98 regression re-run
here). Full criterion 6 sign-off and criteria 1–5 regression-net verification is the tester's job next.
