# T-006: Auth + hub signup + provisioning
<!-- DRAFT by orchestrator at pivot v0.2; cycle-3 planner refines before spawn. -->
Serves: criteria 1,5 (self-signup ≤15 min; Workspace SMTP email)
Role: developer
Model: opus (score: 8/10 — auth is complex-tier per ORCHESTRATION)
Skill: .claude/skills/auth-payments (auth half only — NO payments in V1)
Depends on: T-005
Writes-under: app/web/src/ (auth, signup), supabase/

## Spec
Supabase Auth (email+password, OTP); custom SMTP = Google Workspace help@agapaycare.com (env
names only). /signup: owner account → hub creation → program templates picker (art, swim, karate,
day care, boot camp, open-play, camp) → first class schedule → staff invites. CRM handoff hook:
signup creates crm_hub_profiles row (pipeline entry, alh pattern). Session handling per skill.

## Acceptance checks
- [ ] New owner reaches a populated hub dashboard in ≤15 min unassisted (timed walkthrough)
- [ ] Auth emails arrive via Workspace SMTP on preview deploy; staff invite works end-to-end
