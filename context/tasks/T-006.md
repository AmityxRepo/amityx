# T-006: Auth + hub signup + provisioning
<!-- DRAFT by orchestrator at pivot v0.2; cycle-3 planner refines before spawn. -->
Serves: criteria 1,5 (self-signup ≤15 min; Workspace SMTP email)
Role: developer
Model: opus (score: 8/10 — auth is complex-tier per ORCHESTRATION)
Skill: .claude/skills/auth-payments (auth half only — NO payments in V1)
Depends on: T-005
Writes-under: app/web/src/ (auth, signup), supabase/

## Spec
Supabase Auth (email+password, plus email OTP as a passwordless option); custom SMTP = Google
Workspace help@agapaycare.com configured in the Supabase dashboard (env NAMES only, never in app
code). Signup wizard, each step one job (P.9): (1) owner account + email verify → (2) hub creation
(name → auto-slug, collision-checked) → (3) program-template picker (art, swim, karate, day care,
boot camp, open-play, camp; multi-select with sensible pre-checks) → (4) first class schedule →
(5) optional staff invites by email. Wizard progress persists so a drop-off resumes where it left
off (protects the ≤15-min criterion). On success, land on a populated hub dashboard. CRM handoff:
signup atomically creates the hub's crm_hub_profiles row (onboarding_stage seeded). Session persists
+ auto-refreshes (supabase-js). Error behavior (P.9 rule 8 — never a dead end): duplicate email →
"That email already has an account. Sign in?"; weak password → inline rule; slug collision →
auto-suffix + editable; SMTP send failure → retry + surface the verification path. Staff invitees
are provisioned WITHOUT billing/settings access (enforced by T-005 RLS).

## Assumes / Unknowns
Assumes: T-005 schema (hubs, hub_members, crm_hub_profiles, programs, class_sessions) + RLS are in
place. Program-template contents come from ARCHITECTURE/spec — not founder-blocking.
Unknowns (return `Result: blocked` naming the gap, do NOT guess): the Google Workspace SMTP app
password for help@agapaycare.com must be set in the Supabase dashboard before auth emails send;
Supabase env values (URL + anon key) required. Both are founder-provided.

## Acceptance checks
- [ ] New owner reaches a populated hub dashboard in ≤15 min unassisted (timed walkthrough); a dropped wizard resumes
- [ ] Auth + verification emails arrive via Workspace SMTP on the preview deploy; staff invite works end-to-end and the invitee lands with staff-scoped access only
- [ ] Signup creates the matching crm_hub_profiles pipeline row; duplicate-email / slug-collision / SMTP-failure paths each show a forgiving next step (no dead end)

## Context slice
ARCHITECTURE.md §Data model (hub_members, crm_hub_profiles), §Roles & permissions, §External
services & env vars, §Conventions (UX). DECISIONS D-006 (SMTP), D-007 (CRM handoff), D-012 (P.9).
Skill: auth-payments (AUTH half ONLY — NO payments in V1, per D-003 + criterion-6 guard).
Depends on T-005.

## Result

**Result: done — code-complete AND now fully live-verified, except the one genuinely blocked
sub-piece (Workspace SMTP delivery) named below.**

### Summary
Full signup wizard (account → verify → hub → activities → schedule/commit → invites), session
persistence/auto-refresh, staff-invite flow, and the `provision_hub` atomic RPC (hub + owner
membership + `crm_hub_profiles` row + activities + first class in one transaction) are built,
type-checked, and — now that T-005's migrations are applied to the live Supabase project
(`jjnzbayatcfkkoyorhes`) — **proven end-to-end against the real database**, including the
signup→hub-creation→populated-dashboard round trip and the staff-invite accept flow that were
previously only unit-tested. T-005's RLS deliberately left hub creation with no client INSERT path
("hub creation is a service_role / signup-RPC concern") — T-006 adds the missing SECURITY DEFINER
RPCs (`provision_hub`, `slug_available`, `create_hub_invite`, `resolve_hub_invite`,
`accept_hub_invite`) plus a new `hub_invites` table + RLS in
`supabase/migrations/20260712220000_signup_provisioning.sql`. No payments code anywhere (verified
by review — only Supabase Auth calls).

### Files
- `supabase/migrations/20260712220000_signup_provisioning.sql` — provisioning RPCs + `hub_invites`
- `app/web/src/lib/supabase.ts` — client: `persistSession`/`autoRefreshToken`/`detectSessionInUrl`/`flowType: 'pkce'`
- `app/web/src/auth/{AuthProvider,RequireAuth}.tsx` — session context + `/app` route guard
- `app/web/src/features/signup/{slug,password,authErrors,programTemplates,wizard}.{mjs,d.ts}` —
  pure, unit-tested logic (slug rules, password rule, forgiving auth-error mapping incl. the
  Supabase "empty identities" duplicate-email quirk, the 7-activity template catalog, resumable
  wizard state machine + localStorage persistence, NEVER persists the password)
- `app/web/src/pages/marketing/signup/*` (WizardShell, AccountStep, VerifyStep, HubStep,
  ActivitiesStep, ScheduleStep, InviteStep) + `Signup.tsx` orchestrator (resume-aware)
- `app/web/src/pages/marketing/{Login,ResetPassword,AcceptInvite}.tsx` — sign-in, password reset,
  staff-invite acceptance (email-must-match-invite check, staff-only role forced server-side)
- `app/web/src/pages/app/AppHome.tsx` — populated hub dashboard (hub name, activities, next class)
- `app/web/src/repository/{schema,types,api}.ts` — `provisionHub`, `isSlugAvailable`,
  `create/resolve/acceptHubInvite`, `getMyHub`
- `app/web/src/App.tsx` — wired `/login`, `/reset-password`, `/accept-invite`, `/app` guard
- `app/web/scripts/signup-logic.test.mjs` (14 unit tests, no network), `scripts/auth-smoke.mjs`
  (live Auth smoke, 6 checks), **`scripts/provision-smoke.mjs`** (NEW — live `provision_hub` +
  staff-invite round trip, 22 checks) + `package.json` scripts `test:signup`/`test:auth`/`test:provision`

### How to run
- `cd app/web && npm run build` — tsc + vite build (clean)
- `npm run test:signup` — 14/14, no network
- `npm run test:auth` (needs `.env.local`) — 6/6 against live Supabase Auth
- `npm run test:provision` (needs `.env.local`) — 22/22 against the live DB (self-cleaning)
- `npm run lint:vocab` — 41 files scanned, passes
- Regression: `npm run test:rls` (T-005, 81/0), `npm run test:roster` / `test:attendance` (T-007,
  13/13 + 17/17) — all still green, confirmed after the T-007 merge onto this branch
- Local dev: `npm run dev` → `/signup` walks the full wizard against the now-live schema

### What's live-verified vs. blocked-and-why
**Live-verified (real Supabase, real DB, no mocks — `test:auth` + `test:provision`, 28/28):**
fresh sign-up; duplicate-email via the "empty identities" quirk (confirms confirmations are ON);
weak password rejected client+server; password sign-in; a real signUp to a real inbox
(noel.adv.castillo@gmail.com, cleaned up immediately) returned no SMTP error — the default sender
accepts/queues sends (actual inbox arrival can only be confirmed by the founder; no email-reading
tool here). **NEW this pass:** `provision_hub` atomically creates the `hubs` row, the owner
`hub_members` row, the seeded `crm_hub_profiles` row (onboarding_stage=signup), the chosen
activities, and the first class — all confirmed via the service-role key (ground truth) AND via
the owner's own RLS-scoped client reading the exact dashboard query (`getMyHub()` shape). Staff
invite: create → separate throwaway user accepts → lands with a `hub_members` STAFF row; live RLS
then proves that invitee cannot touch `hubs.plan` (billing), cannot self-grant `owner`, and cannot
read `crm_hub_profiles`, while still being able to do operational work (add a program) — i.e.
"staff-scoped access only" is real, not assumed. Also proved: invite-hijack rejection
(`email_mismatch` for a wrong signed-in email) and idempotent re-accept. All test users/hubs
self-cleaned (verified zero leftover `Smoke Test Hub` rows / `provision.*` auth users afterward).
**Still blocked (named, not guessed):** Workspace SMTP (help@agapaycare.com) delivery — needs the
Google Workspace app password in the Supabase dashboard; without it I can prove the API-level send
succeeds (see above) but not the Workspace-branded delivery the acceptance check requires. This is
a credential gap, not a code gap.

### Objective check
Serves criteria 1 (self-signup ≤15 min, wizard resumable, now proven live end-to-end) and 5
(Workspace SMTP — code path ready and proven up to the send API; branded delivery unverifiable
without the app password). Criterion 6 (no payment processing) held: no payments code added,
confirmed by review of every new file.

### Addendum 2026-07-17 — SMTP now configured but confirmed NOT working (`scripts/smtp-smoke.mjs`)
The founder saved custom SMTP in the Supabase Dashboard (smtp.gmail.com:465, sender
help@agapaycare.com). Ran a live test: (1) fresh signup, (2) resend against a genuinely unconfirmed
user (created via admin API — resend against a phantom email returns a false-positive 200 in ~1ms
without attempting SMTP, so this must be tested against a real unconfirmed user), (3) one attempt
to the founder's own inbox via a `noel.adv.castillo+amityxsmtptest@gmail.com` alias (delivers to
his real inbox; his actual `noel.adv.castillo@gmail.com` account is already a confirmed CRM admin
seeded by T-008 and was NOT touched).

**Result: 0/3 pass.** All three fail identically and immediately with `500
{"error_code":"unexpected_failure","msg":"Error sending confirmation email"}`. Pulled the
underlying error from Supabase's auth logs via the Management API — it is unambiguous:
`535 "5.7.8 Username and Password not accepted... BadCredentials" - gsmtp`. This is Gmail rejecting
the SMTP credentials Supabase has on file — consistent across every attempt (not a transient
platform issue; a real hiccup would not reproduce the identical error 3/3 times). No email reached
any inbox — the founder's inbox check does not apply yet. Supabase does not persist a user row when
the confirmation-send fails, so no cleanup was needed; verified the auth.users list is unchanged
(same 3 pre-existing accounts, nothing added).

**Exact fix needed:** the password field in Supabase Dashboard → Auth → SMTP Settings is wrong for
that username. Gmail/Workspace SMTP relay requires a 16-character **App Password** (not the regular
account password) — generated at myaccount.google.com/apppasswords, which itself requires 2-Step
Verification to be enabled on the help@agapaycare.com Workspace account first. Likely causes: (a) the
regular account password was entered instead of an App Password, (b) the App Password was
mistyped/truncated, or (c) 2-Step Verification isn't enabled so no valid App Password could exist
yet. Re-run `npm run test:smtp` after correcting it — it is a durable, reusable check, not a
one-off script.

Objective check: criterion 6's SMTP sub-item is NOT yet closed — the send path is proven broken,
not proven working. Everything else in this task remains as previously stated.

### Addendum 2026-07-17 (later same day) — re-verified after the founder updated the SMTP password
The founder updated the SMTP password in Supabase and asked for a re-check. Re-ran `npm run
test:smtp` (3/3 fail) plus one dedicated fresh signup to the exact address requested,
`noel.adv.castillo+amityxsmtp2@gmail.com` (also fail — 0/4 total). Pulled fresh log entries for
every attempt via the Management API: **the error is byte-for-byte identical to before the password
change** — `535 "5.7.8 Username and Password not accepted...BadCredentials" - gsmtp`. No email
reached any inbox; no user row persisted (Supabase rolls back user creation when the confirmation
send fails — verified `auth.users` unchanged, still just the 3 pre-existing accounts).

Because the SAME specific error survived a password change, this is no longer just "wrong
password" — it narrows to one of: (a) the newly-entered value still isn't a genuine Gmail App
Password (e.g. the regular account password was re-entered), (b) 2-Step Verification isn't actually
enabled on help@agapaycare.com yet (a hard prerequisite — without it there is no App Password to
generate at all), or (c) the Google Workspace Admin Console is blocking SMTP relay / app passwords
for that account at the org level (would reject any app password identically). Handed these three
specific things to check back to the founder rather than guessing further or re-testing blindly.

### Addendum 2026-07-18 — SMTP CONFIRMED WORKING; criterion 6 CLOSED
The founder re-saved BOTH the SMTP username AND password in Supabase (not just the password this
time). Re-ran `npm run test:smtp`: **3/3 pass** — fresh signup, resend against a real unconfirmed
user, and a founder-inbox alias signup, all clean `200`s. Confirmed via the Management API's
auth-logs endpoint that a successful request logs only `"msg":"request completed","status":200`
with **no** paired `level:error` entry — a structurally different, clean signature versus every
prior failure (which always logged a companion `535 BadCredentials` line). Also sent the specific
dedicated signup requested, `noel.adv.castillo+amityxsmtp3@gmail.com`: `200`, full user object,
`confirmation_sent_at` set, correctly `email_verified:false` (a real, unclicked link, not
admin-shortcut-confirmed). Cleaned up two script-throwaway accounts; left only
`+amityxsmtp3@gmail.com` in place so its real confirmation link stays valid for the founder to
click.

**Likely explanation for the two earlier failures**: since only re-saving the *username* (alongside
the password) fixed it, the SMTP username field was probably the actual culprit earlier — the
password alone being correct couldn't overcome a wrong/malformed username at the SMTP layer, which
is exactly why swapping just the password twice made no difference.

Objective check: criterion 6's SMTP sub-item is now CLOSED. All 6 OBJECTIVE.md v3 criteria are
fully demonstrated live.

### Addendum 2026-07-18 (later) — a SECOND, unrelated bug: Site URL, fixed and verified
The founder clicked the confirmation link from the addendum above and reported it redirected to
`http://localhost:3000/#access_token=...` instead of production — even though the token itself was
valid (`email_verified:true`). This was NOT an SMTP problem; it was Supabase Auth's project-wide
**Site URL** setting, which had been `http://localhost:3000` (the scaffold default) since project
creation, with an empty `uri_allow_list` — unrelated to and predating the SMTP work entirely.

**Fix**: `PATCH https://api.supabase.com/v1/projects/jjnzbayatcfkkoyorhes/config/auth` via the
Management API (`SUPABASE_ACCESS_TOKEN`): `site_url=https://amityx.pages.dev`,
`uri_allow_list=https://amityx.pages.dev/**,http://localhost:3000/**` — confirmed via a fresh GET.
Localhost stays allow-listed (not primary) so local `npm run dev` testing keeps working.

**Code audit**: `AccountStep.tsx` (signup) and the password-reset flow already passed an explicit
`emailRedirectTo`/`redirectTo` — robust regardless of Site URL. `Login.tsx`'s resend-confirmation
call did not, relying purely on Site URL — fixed for consistency (would have silently redirected to
localhost the whole time Site URL was wrong, and would break again for local dev now that Site URL
is pinned to production). Rebuilt, redeployed to Cloudflare Pages production
(`npx wrangler pages deploy dist --project-name amityx --branch main`; confirmed bundle hash
`index-QTsH0vLD.js` live at amityx.pages.dev).

**Verification** (via `supabase.auth.admin.generateLink`, which returns the exact link Supabase
would email without actually sending anything): (1) explicit `redirectTo` matching the app's own
value → action_link's `redirect_to` resolves to `https://amityx.pages.dev/signup?verified=1`,
allowed; (2) no redirectTo at all (the Site-URL fallback path — what a raw API call or the old
Login.tsx code took) → falls back to `https://amityx.pages.dev`, not localhost. Sent one more real
signup, `noel.adv.castillo+amityxsmtp4@gmail.com` (200, real unclicked link, deliberately left
unconfirmed) for the founder's final click-through check.

Objective check: criterion 6 is now FULLY closed — both the send path and the redirect destination
are proven correct.
