# T-004: Design system — app/DESIGN.md + tokens
<!-- DRAFT by orchestrator at pivot v0.2; cycle-3 planner refines before spawn. -->
Serves: criteria 1–5 (one visual language across every UI surface — signup, /app, /crm — enforcing D-012/P.9)
Role: developer
Model: sonnet (score: 6/10 — shared tokens/components, interpretive "warm-not-kids" brief)
Skill: .claude/skills/design-system
Depends on: T-003
Writes-under: app/web/ (DESIGN.md, tokens, base components)

## Spec
One visual language for all three surfaces (warm/professional for hub owners — not a kids-app
aesthetic; parents aren't the buyer). **Section 1 of app/DESIGN.md = the P.9 design law (D-012)
verbatim: ten rules + canonical UI vocabulary table (+ Spanish equivalents chosen to the same
plain-words bar).** Tokens (color/type/spacing/radius/motion), light+dark, component conventions
(buttons — icon+label, verb phrases; forms; cards; tables; badges; empty states that teach).
WCAG AA; ≥44px targets; ≥16px body; Dynamic-Type-tolerant sizing. Favicon + og-image folded in
(image-creation skill).

## Assumes / Unknowns
Assumes: alh-tracker reference repo is at `C:\Projects\alh-tracker` (read-only pattern source
outside this repo); the visual language is driven by the design-system skill + P.9, NOT copied from it. Two locales in scope: English + Spanish (California ICP) — ES is UI
vocabulary only, NOT full i18n plumbing (out of scope here). Kitchen-sink route lives in the T-003
scaffold. No founder-supplied brand palette on file → designer picks within the brief.
Unknowns: none blocking — no external credentials needed for this task.

## Acceptance checks
- [ ] app/DESIGN.md complete with P.9 rules + vocabulary (EN/ES) as Section 1; base components render in a `/dev/kitchen-sink` route
- [ ] AA contrast verified for token pairs; no icon-only primary-action component exists
- [ ] Banned-words checker committed as an npm script (e.g. `lint:vocab`) that greps `src` for the P.9 "Never say" terms — reused by T-009's vocabulary gate

## Context slice
VERBATIM SOURCE for Section 1: `docs/PRODUCT_SPEC.md` §P.9 — the ten rules (~lines 237–259) and the
canonical UI-vocabulary table (~lines 261–273); copy exactly, then add the ES column to the same
plain-words bar. ARCHITECTURE.md §Design system, §Conventions (UX/D-012). DECISIONS D-012 (design
law), D-010 (positioning: toddler/preschool, warm-professional). Skills: design-system (primary),
image-creation (favicon + og-image only — needs the palette, so it comes after tokens). Depends on T-003.

## Result
Result: done (all 3 acceptance checks verified)

**Summary:** Wrote `app/DESIGN.md` (P.9 verbatim + EN/ES vocabulary as §1, then 9 design-system
sections). Picked a warm terracotta/clay accent on warm ivory (light) / warm brown-black (dark) —
no founder palette existed, this is the design call (D-010: warm-professional, not a kids-app).
Wired tokens as CSS variables (`index.css`) + Tailwind theme (`tailwind.config.js`, `darkMode:
'class'`); self-hosted Inter (`@fontsource`); built Button/Label/Input/Textarea/FormField/Card/
Table/Badge/EmptyState (`src/components/ui/`) + `ThemeToggle`; migrated all T-003 stub screens off
hardcoded `brand-*`/`gray-*` onto the semantic tokens; added `/dev/kitchen-sink`; committed
`lint:vocab` + a house/hub SVG mark → favicon/PWA icons/og-image via `npm run gen:assets`.
**Bug caught & fixed during self-verification:** `CrmLayout`'s sidebar originally reused
`bg-foreground`/`text-background`, which invert in `.dark` — toggling dark mode flipped the CRM
sidebar to light while content went dark. Fixed with a dedicated `sidebar`/`sidebar-foreground`
token pair defined once in `:root` only (not overridden in `.dark`), so the CRM rail stays fixed
dark chrome regardless of the theme toggle — documented in DESIGN.md §3 as a caught-bug fix.

**Files:** `app/DESIGN.md` (new); `app/web/src/index.css`, `tailwind.config.js`, `vite.config.ts`
(PWA colors), `index.html` (theme-init script + og:meta) — tokens/config; `src/components/ui/*`
(new, 9 files), `src/components/ThemeToggle.tsx` (new), `src/lib/utils.ts` (new);
`src/pages/dev/KitchenSink.tsx` (new) + `App.tsx` route (exempted from the Supabase setup gate —
static, no data); restyled `SetupNeeded.tsx`, `Landing.tsx`, `Signup.tsx`, `AppHome.tsx`,
`AppStub.tsx`, `CrmHome.tsx`, `CrmStub.tsx`, `AppLayout.tsx`, `CrmLayout.tsx`, `InstallPrompt.tsx`;
`assets/src/*.svg` (mark/mark-maskable/og-image masters, new) + `scripts/gen-assets.mjs` +
`scripts/lint-vocab.mjs` (new) regenerating `public/favicon*`, `public/icon-*`, `public/og-image.png`.

**How to run:**
```
cd app/web
npm install
cp .env.example .env.local   # any non-blank values pass the setup gate
npm run dev                  # http://localhost:5173 — visit /, /signup, /app, /crm, /dev/kitchen-sink
npm run build && npm run preview   # production build check
npm run lint:vocab            # banned-words gate
npm run gen:assets             # regenerate favicon/icons/og-image from assets/src/*.svg after any palette change
```

**Verified:**
1. `npm run build` (tsc + vite) clean; `tsc --noEmit` clean. `npm run preview` served `/`,
   `/signup`, `/app`, `/crm`, `/dev/kitchen-sink` at 200; `manifest.webmanifest` reflects the new
   theme/background colors and regenerated icons (192/512/512-maskable, dimensions confirmed via
   `sharp` metadata).
2. Contrast: computed WCAG relative-luminance ratios with a small Node script (not eyeballed) for
   every token pair in both themes — all ≥4.5:1 text / ≥3:1 UI-boundary pairs pass (ratios tabled
   in DESIGN.md §3, e.g. body 14.63:1/14.91:1, primary-on-primary 5.03:1/5.98:1, focus ring
   4.91–5.85:1, `input` border 3.17–3.68:1). Visually confirmed with Playwright screenshots of
   kitchen-sink (light + dark), `/app` at 375px, and `/crm` at desktop width (light + dark, incl.
   the sidebar-token bug fix above). No icon-only primary-action variant exists — `Button`'s
   `children` prop is required by its TS type; `icon` is optional and leading-only.
3. `lint:vocab`: passes clean on the current codebase (18 UI files scanned); proved it actually
   catches violations with a throwaway fixture file containing "programs"/"guardians" (caught,
   exit 1), then removed it and re-ran clean (exit 0). Scoped to UI copy (JSX text + string
   literals) in marketing/app/dev/components, excluding `/crm` per the vocabulary table's last row.

**Adjacent notes (not fixed):** kitchen-sink's demo checkbox is a native `<input type="checkbox">`
at 20px — fine there since it's paired with a clickable label, but a real Checkbox component with
a 44px hit target should be formalized when a real consent-checkbox screen lands (T-006 signup /
T-007 roster). `npm audit` still flags the same T-003-noted vite@5/vite-plugin-pwa@0.20
dev-server advisory (dev-only, unchanged). Did not commit/open a PR: this is a foundational,
locally-verifiable task (no deploy-like verification needed) and the working tree has other
in-flight, uncommitted context/ changes from a concurrent process — left those untouched and
did not create a branch to avoid interfering with it.

**Objective check:** Serves criteria 1–5 (one visual language, D-012/P.9 enforced) — every future
screen (signup, /app, /crm) now has tokens/components/vocabulary/contrast to build against and a
committed gate (`lint:vocab`) that keeps it honest; unblocks T-005 onward.
