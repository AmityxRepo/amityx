# T-010: Free public booking/waitlist page per hub
<!-- DRAFT by orchestrator at v0.3 ratification (D-009); cycle-3 planner refines before spawn. -->
Serves: criterion 2 (booking page feeds roster) — the owner-revenue hook of the free layer
Role: developer
Model: sonnet (score: 6/10 — anonymous public write path, RLS field exposure, spam surface)
Skill: .claude/skills/web-development (+ ux-design at planning)
Depends on: T-005, T-006
Writes-under: app/web/src/ (public hub pages)

## Spec
Public route per hub (amityx.pages.dev/h/{slug}): hub profile, program cards with schedules,
capacity/waitlist state, booking-request form (child name+age, guardian contact — minimal PII,
no account required; rate-limited, spam-protected). Request → owner notification (email) +
/app booking inbox. "Powered by Amityx" footer (the P.8 loop). SEO-sane (static-ish render),
shareable link for the hub's Instagram bio.

## Spec — clarifying
Public, unauthenticated route `/h/{slug}`: hub profile, "Activities" cards with "Classes" +
schedules, capacity/waitlist state, and a booking-request form (child first name + age band,
guardian name + contact — minimal PII, NO account). Anti-spam WITHOUT any paid captcha or paid SMS:
hidden honeypot field + minimum fill-time + the per-hub/day insert rate limit from T-005; a tripped
honeypot is rejected silently. On submit → insert booking_request → owner notified by EMAIL via
Workspace SMTP (D-014: NO system SMS in V1; the shareable link is "SMS-able" by a human) → appears
in the /app "Requests" inbox. Anonymous visitors can INSERT only; never SELECT roster or other
requests (T-005 RLS). "Powered by Amityx" footer (the P.8 loop). SEO: per-hub title/OG meta,
static-ish first paint. Edge cases: unknown slug → friendly "page not found" (no stack/leak); full
class → "Join waitlist"; duplicate submit → idempotent-friendly thank-you, no error wall.

## Assumes / Unknowns
Assumes: T-005 (booking_requests + anon-INSERT RLS + rate-limit trigger) and T-006 (hub exists,
slug, SMTP) are done; only fields explicitly flagged public are rendered.
Unknowns: none founder-blocking. Delivery is email-only per D-014 (flagged in Result) — do NOT reach
for a paid SMS gateway; it would breach COST_POLICY and stall the task.

## Acceptance checks
- [ ] Anonymous parent submits a request in <60s on mobile; owner sees it in /app inbox + email
- [ ] Full class shows waitlist state; RLS: public page exposes only fields marked public (anon cannot read roster)
- [ ] Honeypot/rate-limit blunts spam without a paid captcha; footer link present; page loads <2s on 3G-class connection

## Context slice
ARCHITECTURE.md §Data model (booking_requests, programs, class_sessions), §Structure (public route),
§Conventions (UX/P.9 — public pages = single scroll, no nav). DECISIONS D-009 (booking hook), D-010
(positioning copy), D-014 (email-only delivery). PRODUCT_SPEC §P.8 (Powered-by loop). Skills:
web-development + ux-design. Depends on T-005, T-006. (Only needs T-005/T-006, so it COULD run
parallel to T-007, but is sequenced after so the /app "Requests" inbox exists to verify the round-trip.)
