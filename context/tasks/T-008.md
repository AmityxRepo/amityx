# T-008: Internal CRM (/crm)
<!-- DRAFT by orchestrator at pivot v0.2; cycle-3 planner refines before spawn. -->
Serves: criterion 5 (internal /crm pipeline, staff-auth only)
Role: developer
Model: sonnet (score: 6/10)
Skill: .claude/skills/web-development
Depends on: T-005, T-006 (parallel with T-007 — different route trees)
Writes-under: app/web/src/ (/crm routes)

## Spec
Desktop-first, crm_admins-gated (unlike alh demo mode — auth REQUIRED from day one). Mirror
alh-tracker CRM: dashboard (pipeline counts by subscription status + onboarding stage, open
follow-ups w/ overdue highlight), hubs list (search/filter/archive toggle), hub detail (profile,
owner contact, onboarding checklist, follow-ups w/ priority+escalation, comm log, admin notes),
add/edit hub modal, archive w/ confirmation. Provisioning action: create hub + owner invite from CRM.
Seed the pipeline with the 10 archetype prospect rows from docs/PILOT_TARGETS.md (stage: prospect).

## Spec — clarifying
Gate /crm at TWO layers: a route guard (redirect non-admins) AND RLS (crm_* readable only by
crm_admins) — the guard is UX, RLS is the real boundary. Left-sidebar nav (P.9 rule 6, desktop);
/crm may use precise internal terms (P.9 exempts our staff surface). The "Create hub + invite
owner" provisioning action reuses the T-006 path (same atomic hub + crm_hub_profiles + owner
invite), so a CRM-created hub is identical to a self-signup hub. Seed = the 10 rows from
docs/PILOT_TARGETS.md "The 10 slots" table, in the listed approach order: onboarding_stage
"prospect", with archetype + free-layer hook in notes; seed is idempotent. Overdue follow-ups
highlighted; archive is reversible (toggle + confirmation stating the consequence).

## Assumes / Unknowns
Assumes: T-005 (crm_admins, crm_hub_profiles, crm_followups, crm_comm_log + RLS) and T-006
(provisioning/invite path) are done. Mine the CRM layout from the reference repo
`C:\Projects\alh-tracker` (read-only, outside this repo — it already HAS a /crm surface) + ARCHITECTURE.
The first platform-admin (crm_admins row) is created via
seed/manual insert tied to the founder's account — name it in the seed.
Unknowns: pilot business NAMES are still placeholders (founder fills per the PILOT_TARGETS sourcing
playbook + confirms metro) — seed the 10 ARCHETYPE rows now; real names slot in later, NOT a blocker.

## Acceptance checks
- [ ] Non-crm_admin user cannot reach /crm data (route guard AND RLS); hub_owner/hub_staff both denied
- [ ] Full pipeline walk: add hub → stage transitions → provision → appears in hub app
- [ ] Seed loads the 10 archetype prospect rows idempotently; overdue follow-ups highlighted

## Context slice
ARCHITECTURE.md §Data model (crm_* tables), §Roles & permissions (platform_admin), §Structure (/crm
tree). docs/PILOT_TARGETS.md (the 10-slot table + "CRM seeding" note). DECISIONS D-007, D-010
(pipeline fields/positioning), D-013 (seed source). Skill: web-development. Depends on T-005, T-006.
Parallel with T-007 (disjoint route trees).
