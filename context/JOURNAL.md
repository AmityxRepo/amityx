# Activity Journal — Amityx founding product spec

Format: `<YYYY-MM-DD HH:MM> · c<cycle> · <actor> · <type> · <item> · <status> — <one line> [→ file]`

## Cycle 1
2026-07-11 18:35 · c1 · orchestrator · intake · framework · done — Forge copied into Amityx, git init + initial commit
2026-07-11 18:40 · c1 · orchestrator · intake · OBJECTIVE · done — founding product spec, 6 criteria; tier complex (future app), cycle 1 non-app path
2026-07-11 18:40 · c1 · orchestrator · decision · D-001,D-002 · done — non-app path; inline production (no spawns per host policy)
2026-07-11 18:40 · c1 · orchestrator · plan · T-001 · backlog — produce docs/PRODUCT_SPEC.md → tasks/T-001.md
2026-07-11 18:45 · c1 · orchestrator · research · R-001 · done — 4 web searches; key finding: AI family-assistant wave (Ohai/Milo/Skylight) contests the wedge → research/R-001.md
2026-07-11 18:50 · c1 · orchestrator · produce · T-001 · in-progress — writing spec in 5 chunks
2026-07-11 19:10 · c1 · orchestrator · produce · T-001 · done — 970-line spec: wedge=Child Inbox, moat=child graph + provider claim loop → docs/PRODUCT_SPEC.md
2026-07-11 19:15 · c1 · orchestrator · test · T-001 · pass — grep verification: 12× closers, 14×5 competitor dims, 9×5 persona attrs; Mermaid hardened; all 6 criteria ✓
2026-07-11 19:15 · c1 · orchestrator · blocker · cycle-2 · blocked — awaiting founder ratification (spec §13.4) + LLM API key decision

## Cycle 1 — closed 2026-07-11: founding product spec delivered & verified; 0 bugs; criteria 1–6 met; cycle 2 gated on founder decisions

## Cycle 2
2026-07-11 20:05 · c2 · user · intake · pivot · done — founder decisions: provider-pays (toddler activity hubs), AI on hold, web+PWA no stores, $0 infra (pinned accounts), mirror alh-tracker flow
2026-07-11 20:10 · c2 · orchestrator · research · alh-tracker · done — three-surface model studied (CRM/tenant app/family-portal-later; React+Vite+Supabase+Vercel, PWA, Playwright)
2026-07-11 20:15 · c2 · orchestrator · research · R-002 · done — provider-side competitors + pricing; gap = multi-activity toddler hubs → research/R-002.md
2026-07-11 20:20 · c2 · orchestrator · decision · D-003..D-007 · done — pivot, AI hold, PWA/no-stores, pinned infra, alh-mirror
2026-07-11 20:30 · c2 · orchestrator · produce · T-002 · done — OBJECTIVE v2, ARCHITECTURE v1, spec v0.2 (pivot section + 7 banners), INDEX → docs/PRODUCT_SPEC.md
2026-07-11 20:35 · c2 · orchestrator · plan · T-003..T-009 · backlog — V1 build drafts (planner refines at cycle-3 PLAN) → tasks/
2026-07-11 20:40 · c2 · orchestrator · blocker · cycle-3 · blocked — founder "go" + P.7 answers (age band, first-10 list, pricing)

## Cycle 2 — closed 2026-07-11: pivot v0.2 applied (spec+context+backlog); no code per user instruction; cycle 3 = V1 build on founder go

## Cycle 2 addendum
2026-07-11 21:10 · c2 · user · plan · GTM · in-progress — founder raises adoption-friction objection; proposes ClassDojo-style penetration
2026-07-11 21:20 · c2 · orchestrator · design · spec P.8 · done — PROPOSED GTM: free layer (booking page + parent broadcast/photos + kiosk check-in, zero during-class process), owner stays payer; awaiting ratification → docs/PRODUCT_SPEC.md §P.8
2026-07-11 21:40 · c2 · orchestrator · research · infra pricing · done — verified: Supabase Pro $25/mo, Vercel Pro $20/user/mo (Hobby bans commercial use, enforced); Cloudflare Pages = free commercial alt → R-002 (P.7 watch item closed)
2026-07-11 22:00 · c2 · user · decision · D-008 · done — Cloudflare account created; production = Pages + R2 media; Supabase stays free until first paying hub
2026-07-11 22:10 · c2 · orchestrator · research · R-003 · done — capacity model: ~25–50 hubs / ~2.5–7.5k parents on $0 infra (Supabase JSON egress binds); T-003/T-009/ARCHITECTURE/OBJECTIVE updated → research/R-003.md
2026-07-11 22:30 · c2 · user · decision · deferral · done — founder: Cloudflare account is FREE tier; defers open decisions to orchestrator recommendations
2026-07-11 22:35 · c2 · orchestrator · research · cloudflare free · done — verified: Pages unlimited bandwidth no card; R2 REQUIRES card on file → staged media plan → R-003 addendum
2026-07-11 22:40 · c2 · orchestrator · decision · D-009..D-011 · done — P.8 GTM ratified; ICP toddler/preschool 0–5 core; pricing $49 launch/$79 list; staged media (Supabase Storage → R2 @ ~5 hubs)
2026-07-11 22:45 · c2 · orchestrator · plan · v0.3 · done — spec v0.3, OBJECTIVE v3 (6 criteria), T-005/T-007 re-scoped, T-010/T-011 drafted; remaining founder inputs: first-10 list + "go"
2026-07-11 23:00 · c2 · user · decision · D-012 · done — founder directive: ease-of-use is law; owners/staff/parents must never be confused by features/buttons/flows
2026-07-11 23:10 · c2 · orchestrator · design · spec P.9 · done — v0.4: ten testable rules + canonical UI vocabulary + tester gates; wired into OBJECTIVE constraints, ARCHITECTURE conventions, T-004 (DESIGN.md section 1), T-009 (usability gates + hallway test)
2026-07-11 23:30 · c2 · user · decision · D-013 · done — GO for cycle-3 build (via dispatched session); first-10 list delegated to orchestrator
2026-07-11 23:40 · c2 · orchestrator · plan · pilot targets + kickoff · done — docs/PILOT_TARGETS.md (10 archetype slots, sourcing playbook, outreach script, CRM seed) + docs/BUILD_KICKOFF_PROMPT.md; T-008 seeds pipeline from it

## Cycle 2 — closed 2026-07-11 (final): strategy complete at spec v0.4 (D-001..D-013); cycle 3 = V1 build, dispatch-ready

## Cycle 3 — V1 build (dispatched session)
2026-07-12 09:00 · c3 · orchestrator · intake · boot · done — read INDEX/OBJECTIVE/STATE/PROGRESS/JOURNAL tail/DECISIONS/T-003..T-011; app/ + supabase/ confirmed empty (greenfield); node v20.20/npm/git present, gh + supabase CLI not installed (install on demand)
2026-07-12 09:05 · c3 · orchestrator · plan · T-003..T-011 · in-progress — spawned planner (opus) to refine 9 draft tasks, no rescope
2026-07-12 09:20 · c3 · planner · plan · T-003..T-011 · done — refined all 9 task files (Assumes/Unknowns, Context slice, corrected Serves on T-005→crit.6, T-007→crit.3, T-008→crit.5), added T-010/T-011 to T-009 deps, confirmed model scores unchanged, flagged tester must run opus on T-009 (security cycle)
2026-07-12 09:20 · c3 · planner · decision · D-014 · done — V1 link delivery is email-only ("SMS" = human-pasted link); no paid SMS gateway → DECISIONS.md
2026-07-12 09:25 · c3 · orchestrator · develop · T-003 · in-progress — spawned developer (sonnet)
2026-07-12 10:35 · c3 · developer · develop · T-003 · done — app/web scaffolded (React18+TS+Vite+Tailwind+Zustand+Router, 4 route trees, PWA manifest+SW, repository layer); build clean, routes verified, blank-env setup screen verified; committed locally `feature/T-003` (b818075) — GitHub push blocked (403, no write access for llllollki) → context/tasks/T-003.md
2026-07-12 10:35 · c3 · orchestrator · blocker · github-push · blocked — push to github.com/AmityxRepo/amityx denied; local commits proceeding, will surface with any other blockers
2026-07-12 10:40 · c3 · orchestrator · develop · T-004 · in-progress — spawned developer (sonnet)
2026-07-12 12:05 · c3 · developer · develop · T-004 · done — app/DESIGN.md (P.9 verbatim + EN/ES vocab §1, tokens, 9 base components, kitchen-sink route, lint:vocab); warm terracotta/ivory palette chosen (no founder brand on file); AA contrast computed for all token pairs; caught+fixed a dark-mode CRM sidebar inversion bug pre-emptively → context/tasks/T-004.md
2026-07-12 12:10 · c3 · orchestrator · develop · T-005 · blocked — checked locally before spawning developer: no Docker (no local Supabase alt.), no live Supabase credentials anywhere in env (.env.local has placeholder values only), `supabase` CLI not installed. Migration SQL could be authored blind, but the acceptance check requires adversarially PROVING RLS isolation against a real DB — cannot verify without a live project. Stopping cycle 3 here to report blocker to founder rather than spend an opus spawn confirming already-known facts.

## Cycle 3 resumed 2026-07-12 — founder supplied Supabase env values (.env.local) + GitHub repo AmityxRepo/amityx now exists
2026-07-12 14:20 · c3 · orchestrator · unblock · T-005 · done — verified real VITE_SUPABASE_URL/ANON_KEY/SUPABASE_SERVICE_ROLE_KEY in app/web/.env.local; npx supabase v2.109.1 works; ran `supabase init` (no auth needed) → supabase/config.toml
2026-07-12 14:20 · c3 · orchestrator · attempt · github-push · blocked — repo now exists but `git push` still 403 for account llllollki (Permission denied to AmityxRepo/amityx) — distinct from repo-not-existing; needs collaborator/write access grant for that account
2026-07-12 14:25 · c3 · orchestrator · develop · T-005 · in-progress — spawned developer (opus) with pre-diagnosed credential gap (supabase link fails: needs SUPABASE_ACCESS_TOKEN, distinct from anon/service-role keys; direct psql needs DB password, also distinct)
2026-07-12 15:00 · c3 · developer · develop · T-005 · blocked (DDL-apply only) — 4 migrations authored (20 tables, RLS enabled+forced, deny-by-default, SECURITY DEFINER membership helpers, guardian_links RPCs, booking_requests anon-insert+rate-limit, crm_* isolation, support-access audit); adversarial suite (2 hubs/4 principals) + idempotent seed/teardown + rollback runbook all written and self-checked; reconfirmed DDL apply needs SUPABASE_ACCESS_TOKEN or DB password (neither supplied); committed `feature/T-005` (5e7611d) → context/tasks/T-005.md, supabase/migrations/*, supabase/README.md
2026-07-12 15:05 · c3 · orchestrator · fix · git-mistake · done — an inspection `git checkout feature/T-004 -- .` accidentally reverted 4 already-committed T-005 files in the working tree; restored via `git checkout HEAD --` before any commit was made, no history impact
2026-07-12 15:10 · c3 · orchestrator · develop · T-006 · in-progress — spawned developer (opus) on `feature/T-006`; agent hit a session API limit mid-task (cut off after building most of the signup wizard) — resumed from its own transcript via SendMessage once the limit reset, no rework needed
2026-07-12 19:25 · c3 · developer · develop · T-006 · done (code-complete; 2 named blockers) — signup wizard (account→verify→hub→activities→schedule→invites) + AuthProvider/RequireAuth + provision_hub SECURITY DEFINER RPC (hub creation had no client INSERT path under T-005 RLS by design) + hub_invites table/RLS + staff-invite accept flow; 14/14 logic unit tests, 6/6 live Supabase-Auth smoke tests, clean build/vocab-lint; committed `feature/T-006` (84ce0f5) → context/tasks/T-006.md
2026-07-12 19:25 · c3 · orchestrator · verify · T-006-email · noted — smoke test performed one real signUp call to the founder's own email (noel.adv.castillo@gmail.com) to prove the default Supabase sender queues sends with no SMTP error; test user immediately deleted via admin.deleteUser; actual inbox delivery unconfirmed (agent has no email-reading tool) — flagged to founder for awareness, not a data-safety issue
2026-07-12 19:25 · c3 · orchestrator · attempt · github-push · blocked — re-tested push to AmityxRepo/amityx (repo now exists) — still 403 for llllollki; same permissions ask as before, unaffected by repo creation
2026-07-12 19:30 · c3 · orchestrator · blocker · T-006-smtp · blocked — Workspace SMTP app password (help@agapaycare.com) needed in Supabase Dashboard → Auth → SMTP settings before Workspace-branded delivery can be proven; API-level send already confirmed working via Supabase's default sender

## Cycle 3 resumed — founder supplied SUPABASE_ACCESS_TOKEN; GitHub push clarified (AmityxRepo, not llllollki)
2026-07-12 21:30 · c3 · orchestrator · unblock · T-005-ddl · done — confirmed SUPABASE_ACCESS_TOKEN present in app/web/.env.local; `npx supabase link --project-ref jjnzbayatcfkkoyorhes` succeeded; `npx supabase db push` applied all 5 migrations to the live project (`supabase migration list --linked` confirms local=remote for all 5); `npm run seed` succeeded
2026-07-12 21:45 · c3 · orchestrator · test · T-005-rls-live · fail (1/80) — ran `npm run test:rls` against the live DB: 79 passed, 1 failed ("valid token child scoping — got []"). Diagnosed directly (2 isolated reproductions): NOT an RLS/RPC bug — `seedHub()`'s child_guardians batch insert mixed an explicit `is_primary:true` (row1) with an omitted `is_primary` (row2); PostgREST's multi-row insert sends explicit NULL for the omitted key instead of applying the column default, so the whole 2-row insert hit a NOT NULL violation (23502) that the test never checked — child_guardians silently never got populated for either child in the affected fixture
2026-07-12 21:50 · c3 · orchestrator · fix · T-005-rls-live · in-progress — spawned developer (sonnet) with full root-cause + exact fix already diagnosed
2026-07-12 22:05 · c3 · developer · fix · T-005-rls-live · done — fixed both child_guardians rows to set is_primary explicitly + added insertChecked() helper so future silent insert failures throw loudly; 2 consecutive clean live runs = 81/0 (some transient failures noted during iteration, attributed to concurrent live-DB access from parallel T-007/T-008 background agents, not the fix); committed `feature/T-005` (dfcb307) → context/tasks/T-005.md
2026-07-12 22:10 · c3 · orchestrator · merge · T-005→T-006 · done — merged feature/T-005 (dfcb307) into feature/T-006 (clean, no conflicts) so the RLS fix carries forward into the active branch
2026-07-12 22:10 · c3 · orchestrator · verify · github-token · done — checked app/web/.env.local + repo root for GITHUB_TOKEN/GH_TOKEN/PAT — none present. Per founder instruction, did NOT push as llllollki; confirmed remote is exactly https://github.com/AmityxRepo/amityx.git. Blocker stands: need a GitHub PAT with write access to AmityxRepo/amityx

## Cycle 3 resumed — founder supplied GITHUB_TOKEN (PAT with write access)
2026-07-12 23:00 · c3 · orchestrator · unblock · github-push · done — confirmed GITHUB_TOKEN present; pushed master + feature/T-003..T-006 to AmityxRepo/amityx via inline x-access-token URL (never persisted to git config); ls-remote confirms all 5 refs match local; opened PRs #1-#4 (T-003..T-006) against master via GitHub API (curl, no gh CLI available)
2026-07-12 23:10 · c3 · developer(T-007) · develop · T-007 · done — /app: 4-tab nav (Today·Roster·Requests·More), kiosk mode (parent tiles, staff long-press exit), staff fallback check-in/out (offline-tolerant, idempotent by session+child+action — 17 unit tests on the queue/reducer), daily notes (≤3 taps, quick-tags), children/guardian CRUD, requests inbox (accept→auto-enroll w/ capacity→waitlist, undoable decline); 44 total unit tests + build + vocab-lint green; committed `feature/T-007` (9f73826, 56e3039, 132713d) off T-006's tip → context/tasks/T-007.md
2026-07-12 23:15 · c3 · orchestrator · merge · T-007→integration · done — merged feature/T-007 into working branch, clean, no conflicts; re-ran build + test:attendance (17/17) + test:roster (13/13) + lint:vocab (41 files) — all green; pushed feature/T-007 (unmerged, clean tip 132713d) to origin, opened PR #5
2026-07-12 23:10 · c3 · orchestrator · unblock · T-006-finalize · in-progress — schema now live, so provision_hub round-trip is provable; resumed the T-006 developer agent (same session, full context) to add a live provisioning test + staff-invite live test and finalize Result
2026-07-12 23:20 · c3 · developer(T-008) · develop · T-008 · done — /crm: two-layer gate (route guard + RLS), dashboard (pipeline counts, overdue follow-ups), hubs list, hub detail (profile/checklist/followups/comm-log/notes), add/edit modal, "Create hub + invite owner" provisioning (new crm_provision_hub/crm_invite_hub_owner RPCs, additive migration applied live), idempotent 10-archetype seed + founder crm_admins row; independently discovered the live SUPABASE_ACCESS_TOKEN and used it to apply its own migration + prove every acceptance check live (not just logic); test:crm 14/14, test:crm:live 14/14, test:rls 89/90 (1 failure = the same pre-existing guardian-link fixture bug already fixed on feature/T-005, just stale in T-008's isolated worktree — not a new regression); committed `feature/T-008` (not pushed yet) → context/tasks/T-008.md
CORRECTION: the T-007 line above claims "opened PR #5" — that never actually happened (branch was pushed, no PR created via the API). No functional impact: T-007's commits reached master anyway via the PR #4 merge chain below.

## Cycle 3 resumed — usage-limit reset; founder added Cloudflare Pages + R2 credentials (R2 not used yet, D-011 still staged)
2026-07-13 00:10 · c3 · developer(T-006) · finalize · T-006 · done — live-verified provision_hub round trip (22/22: atomic hub+owner+CRM-row+activities+class creation, owner RLS-scoped dashboard read, staff-invite create→accept→staff-only access, invite-hijack rejection, idempotent re-accept); full regression green (build, signup 14/14, auth 6/6, rls 81/0, roster 13/13, attendance 17/17); committed `feature/T-006` (38766a0)
2026-07-13 00:15 · c3 · orchestrator · merge · T-008→integration · done — merged feature/T-008 into the integration branch; 3 real conflicts (package.json scripts, repository/types.ts, repository/api.ts) — all purely additive (T-007's session/roster/attendance/notes surface vs. T-008's CRM surface, zero name collisions), resolved by union; mistakenly `git add -A`'d the two agent worktree dirs as embedded repos — caught immediately, untracked + gitignored in a follow-up commit
2026-07-13 00:25 · c3 · orchestrator · test · post-merge-regression · pass — build clean, lint:vocab (41 files), test:rls 90/0 (81 base + T-008's +9, guardian-link fix confirmed carried through), test:signup 14/14, test:auth 6/6, test:provision 22/22, test:attendance 17/17, test:roster 13/13, test:crm 14/14, test:crm:live 14/14, seed + seed:crm both idempotent
2026-07-13 00:30 · c3 · orchestrator · unblock · github-consolidate · done — founder authorized merging PRs in dependency order for clean history; pushed the fully-merged feature/T-006 branch, merged PR #4 into master via GitHub API (commit 348f1a3) — PRs #1/#2/#3 (T-003/T-004/T-005) auto-closed as merged (their commits are now master ancestors); local master fast-forwarded to match

## Cycle 3 continues — usage-limit reset; founder added Cloudflare Pages + R2 credentials (R2 staged, not used, D-011)
2026-07-13 00:45 · c3 · orchestrator · develop · T-010 · in-progress — spawned developer (sonnet) on feature/T-010 (from consolidated master); flagged the anon-read gap in advance (T-005's RLS has no anon SELECT on hubs/programs/class_sessions) and named the exact fix pattern (curated SECURITY DEFINER RPC, mirroring resolve_guardian_link) so the developer didn't have to discover or invent it
2026-07-13 02:10 · c3 · developer(T-010) · develop · T-010 · done — new `get_public_hub_page(slug)` RPC (curated public fields only) applied live; /h/{slug} public route (single-scroll, no nav), booking form, honeypot+min-fill-time anti-spam (silent drop, no paid captcha), waitlist state, Powered-by-Amityx footer, per-hub title/OG meta; test:booking 10/10, test:booking:live 20/20, test:rls 95/95 (was 90, +5 new anon-boundary checks), full regression green; committed `feature/T-010` (f3acd3a) → context/tasks/T-010.md
2026-07-13 02:15 · c3 · orchestrator · verify · T-010-regression · pass — re-ran build/lint:vocab/test:rls myself: 95/95, clean
2026-07-13 02:20 · c3 · quick-fix · fix · T-010-copy · done — HubStep.tsx booking-URL hint was missing the `/h/` path segment (T-010 follow-up note); one-line fix, build+lint:vocab reverified; committed `feature/T-010` (500cd80)
2026-07-13 02:20 · c3 · orchestrator · note · T-010-followups · noted (non-blocking) — owner email notification for new booking requests not wired (Requests inbox still works via in-app read); OG meta is CSR-only (no prerender); Cloudflare Pages SPA-fallback (_redirects) not yet configured for deep routes — carrying this into T-009's deploy step
2026-07-13 02:25 · c3 · orchestrator · integrate · T-010→master · done — pushed feature/T-010, opened PR #5, merged into master (commit 53ecd21); local master fast-forwarded
2026-07-13 02:30 · c3 · orchestrator · develop · T-011 · in-progress — spawned developer (opus) on feature/T-011; flagged 2 real schema/architecture gaps in advance (no multi-child join table for group-photo consent; no mechanism to mint signed Storage URLs for an anonymous guardian since there's no separate backend) with a recommended default for each, explicitly inviting the developer to deviate with a documented reason
2026-07-13 04:15 · c3 · developer(T-011) · develop · T-011 · done — migration applied live (photo_moment_children join+RLS, announcements.image_path, 30-day expiry trigger, create_photo_moment/get_guardian_feed/mark_guardian_announcements_read RPCs, private photo-moments bucket); deployed Edge Function `guardian-media` (via --use-api, no Docker) for token-scoped signed-URL minting — correctly IMPROVED on my suggested anon-SELECT-policy default, reasoning that a policy broad enough to sign a known path also enables bucket enumeration via list(), unacceptable for child media; group-photo consent = reject-at-write (matches my recommended default); purge = GitHub Actions cron + script (not pg_cron — pure SQL would orphan storage bytes); parent view /g/:token, staff capture /app/share (client webp compress), family-link issuance on ChildDetail, adapter is a 1-line R2 flip; committed `feature/T-011` (30f4bfd) → context/tasks/T-011.md. Flagged: the purge GitHub Actions workflow needs SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY as repo secrets before its cron can run — deferring to T-009 to batch-configure alongside Cloudflare/keep-alive secrets rather than set piecemeal now
2026-07-13 04:20 · c3 · orchestrator · verify · T-011-regression · pass — re-ran myself: build clean, lint:vocab 44 files, test:rls 98/98, test:signup 14/14, test:auth 6/6, test:provision 22/22, test:attendance 17/17, test:roster 13/13, test:crm 14/14, test:booking 10/10, test:media 28/28 (guardian feed scoping, write+read consent enforcement incl. group photos, signed/private-bucket proof, live 30-day purge via manipulated taken_at, aggregate-only view counts) — all green, zero regressions across the whole 6-task build
2026-07-13 04:25 · c3 · orchestrator · integrate · T-011→master · done — pushed feature/T-011, opened PR #6, merged into master (commit 5217328); local master fast-forwarded

## Cycle 3 continues — usage-limit reset; final task T-009 (e2e + deploy), split developer(deploy)/tester(e2e+audit) per task spec
2026-07-13 05:00 · c3 · orchestrator · develop · T-009-deploy · in-progress — spawned developer (sonnet) for the deploy half only: Cloudflare Pages production, SPA-fallback, keep-alive cron, GitHub Actions repo secrets
2026-07-13 05:30 · c3 · developer(T-009) · develop · T-009-deploy · done — LIVE at https://amityx.pages.dev (verified: real Supabase host baked into the production bundle, not a placeholder; deployed bundle hash-matches local build); `public/_redirects` fixes T-010's flagged deep-route 404 gap; `.github/workflows/keep-alive.yml` (twice-weekly Supabase ping, defeats the 7-day free-tier pause); GitHub repo secrets SUPABASE_URL/VITE_SUPABASE_ANON_KEY/SUPABASE_SERVICE_ROLE_KEY set via API (libsodium sealed-box, values never printed) — unblocks both keep-alive.yml and T-011's purge-media.yml; $0 guardrail grep clean (no Stripe/payment SDK, no R2 usage in src/); test:rls 98/98 post-change; committed `feature/T-009` (66c6ede, c11b933) → context/tasks/T-009.md, app/web/README.md
2026-07-13 05:35 · c3 · orchestrator · integrate · T-009-deploy→master · done — pushed feature/T-009, opened PR #7, merged into master (commit 8dc0de6) WITHOUT waiting for the tester half — the two cron workflows only fire from the default branch, so getting them live took priority; will do a second merge once the tester's e2e suite lands; confirmed via API: 0 open PRs, both `keep-alive` and `purge-media` workflows show `active` on GitHub
2026-07-13 05:40 · c3 · orchestrator · test · T-009-tester · in-progress — spawned tester (opus, security-cycle routing) for the e2e + adversarial + P.9-usability half; live URL https://amityx.pages.dev
2026-07-13 08:50 · c3 · tester(T-009) · test · T-009-tester · pass — built Playwright e2e (15/15 vs production, self-cleaning): smoke/PWA, Journey A (owner self-serve), Journey B (CRM→provision→invite→accept), Regression C (booking→inbox), Regression D (guardian consent-scoping). RLS suite grew to 100/100 + 6/6 novel adversarial probes (auth.users hidden, tokens unforgeable, no self-escalation to crm_admin, append-only audit holds, anon sealed from hub_invites + private bucket). P.9: vocab clean, zero icon-only buttons, zero serious/critical axe on every customer-facing screen at 375/768/1280 light+dark, 3-tap jobs all ≤3, 5-second-test proxy documented. Found+fixed B-001 (S2: /crm showed blank hub names — RLS gap, `hubs_crm_read` policy added, tenant DATA still isolated) and B-003 (S3: dev placeholder text live on production landing) — both redeployed. B-002 (S3, /crm-only a11y: contrast + unlabeled select + small chevrons) partially fixed (select label), contrast+chevrons deferred as non-blocking design-token work. Wrote context/HALLWAY_TEST.md. All 6 objective criteria GREEN. → context/tests/TR-009.md, context/tasks/T-009.md, context/bugs/B-001..003.md
2026-07-13 08:55 · c3 · orchestrator · verify · T-009-tester · pass — independently reproduced: build clean, lint:vocab 44 files, test:rls 100/100, full Playwright suite 15/15 (same P.9 findings reproduced exactly)
2026-07-13 09:00 · c3 · orchestrator · fix · migration-tracking · done — B-001's fix migration (20260713150000) was applied to prod via the Management API directly (not `supabase db push`), leaving the CLI's migration-tracking table out of sync (`migration list --linked` showed local≠remote); re-ran `db push` to apply it through the normal path (idempotent DROP POLICY IF EXISTS+CREATE POLICY, safe to re-run) — now local=remote on all 9 migrations; test:rls re-confirmed 100/100 after reconciliation
2026-07-13 09:05 · c3 · orchestrator · decision · D-015 · done — logged the Edge Function signing pattern + reject-at-write consent + GitHub-Actions-cron-over-pg_cron decisions from T-011/T-009 → DECISIONS.md

## Cycle 3 — CLOSED 2026-07-13: V1 build complete. Live at https://amityx.pages.dev. All 9 tasks (T-003..T-011, T-009) done, merged, deployed. All 6 OBJECTIVE.md v3 criteria demonstrated (5 fully green; criterion 5's Workspace-SMTP email sub-item stays open pending the founder's app password — Supabase's default sender covers it functionally in the meantime). RLS isolation proven adversarially at 100/100 + 6 novel penetration probes. P.9 usability gates automated and passing on every customer-facing screen. Zero S1/S2 bugs open. $0/month infrastructure confirmed (no payment/AI code, no paid tier toggled). Hallway-test protocol documented for the founder to run before public launch.

## Post-close verification 2026-07-17 — founder saved custom SMTP; confirmed it does NOT work yet
2026-07-17 · c3-post · orchestrator · verify · smtp-live · fail (0/3) — founder configured Supabase custom SMTP (smtp.gmail.com:465, help@agapaycare.com, sender name "Amityx"). Wrote + ran `app/web/scripts/smtp-smoke.mjs` (`npm run test:smtp`) against production: (1) fresh signup, (2) resend against a genuinely-unconfirmed admin-created user (resend against a phantom email is a false-positive 200 in ~1ms without attempting SMTP — caught and corrected this in the script before trusting the result), (3) one attempt to the founder's inbox via a Gmail `+tag` alias (his real noel.adv.castillo@gmail.com account is already a confirmed CRM admin seeded by T-008 — not touched). All 3 failed identically: `500 {"error_code":"unexpected_failure","msg":"Error sending confirmation email"}`. Pulled the underlying error from Supabase's auth logs via the Management API: `535 "5.7.8 Username and Password not accepted...BadCredentials" - gsmtp` — a genuine, consistent Gmail SMTP-auth rejection (reproduced 3/3, not a transient platform issue). No email reached any inbox. Supabase doesn't persist a user row when the confirmation-send fails, so no test cruft was created (verified auth.users unchanged: same 3 pre-existing accounts). Diagnosis: Supabase likely has the regular account password on file instead of a Gmail App Password (requires 2-Step Verification enabled on the Workspace account first). Committed the smoke test (`fix/T-006-smtp-verify`, 1e8d61d), pushed, opened PR #9, merged into master (337aa27) — it's a durable, re-runnable check for whenever the founder corrects the credential. Criterion 6's SMTP sub-item stays open, now with an exact, actionable fix instead of "just missing."

2026-07-17 · c3-post · orchestrator · verify · smtp-live-retry · fail (0/4) — founder updated the SMTP password in Supabase and asked for a re-check. Re-ran `npm run test:smtp` (3/3 fail, identical to before) + one dedicated fresh signup to the exact address requested, `noel.adv.castillo+amityxsmtp2@gmail.com` (4th attempt, also fail). Pulled fresh log entries for every attempt via the Management API: **same exact error, unchanged** — `535 "5.7.8 Username and Password not accepted...BadCredentials" - gsmtp`. The password update did NOT fix it. No email sent to any inbox; confirmed (again) that Supabase persists no user row on a failed send — auth.users unchanged (3 pre-existing accounts, nothing new). Since the SAME specific Gmail error persists after a password change, the likely causes narrow to: (a) the new value still isn't a genuine 16-char Gmail App Password (e.g. the regular account password was re-entered), (b) 2-Step Verification isn't actually enabled on help@agapaycare.com yet (a prerequisite for App Passwords to exist at all), or (c) a Workspace admin-console policy is blocking app passwords/SMTP relay for that account org-wide. Reported precisely to the founder with these narrowed next steps. Criterion 6's SMTP sub-item remains open.

2026-07-18 · c3-post · orchestrator · verify · smtp-live-success · **PASS (3/3, +1 dedicated)** — founder re-saved BOTH the SMTP username and password. Re-ran `npm run test:smtp`: all 3 checks pass (fresh signup, resend against a real unconfirmed user, founder-inbox alias) with clean `200`s and NO paired error log entry (every prior failure logged a companion `535 BadCredentials` line; these don't). Also sent the specific dedicated signup requested, `noel.adv.castillo+amityxsmtp3@gmail.com` — `200`, full user object with `confirmation_sent_at` set, `email_verified:false` (correctly awaiting the real click). Confirmed via the Management API auth-logs endpoint: the successful request logs only `"msg":"request completed","status":200` with NO accompanying `level:error` entry — a clean, unambiguous send. Cleaned up the two script-throwaway accounts (`smtp.check.*` auto-cleaned by the script; the generic `+amityxsmtptest<timestamp>` alias from the script's step 3 manually deleted to avoid the founder getting two different confirmation emails to hunt through) — left ONLY `noel.adv.castillo+amityxsmtp3@gmail.com` in place, unconfirmed, with its real emailed link intact for the founder to click. **Criterion 6's SMTP sub-item is now GREEN — all 6 OBJECTIVE.md v3 criteria fully demonstrated live.** Founder asked to check noel.adv.castillo@gmail.com (and Spam) for the confirmation email.

2026-07-18 (later) · c3-post · orchestrator · fix · auth-site-url · done — founder reported the confirmation link (from the +amityxsmtp3 email above) redirected to `http://localhost:3000/#access_token=...` instead of production — confirming the email actually landed and the token was valid (`email_verified:true`), but Supabase Auth's project-wide **Site URL** had never been changed from its scaffold default. Confirmed via `GET /v1/projects/.../config/auth`: `site_url=http://localhost:3000`, `uri_allow_list=""`. Fixed via `PATCH` (Management API, `SUPABASE_ACCESS_TOKEN`): `site_url=https://amityx.pages.dev`, `uri_allow_list=https://amityx.pages.dev/**,http://localhost:3000/**` (kept localhost allowed, not primary, for local dev testing) — confirmed via a fresh GET. Checked app code: `AccountStep.tsx` (signup) and `ResetPassword`'s flow already passed explicit `emailRedirectTo`/`redirectTo`, but `Login.tsx`'s resend-confirmation call didn't — meaning it silently depended entirely on Site URL and would have kept working incorrectly (redirecting to localhost) until this fix, then would break again for local dev now that Site URL is pinned to production. Fixed for consistency (`fix/auth-redirect-url`, d14803c → PR #10 → merged f127fa6), rebuilt, redeployed to Cloudflare Pages production (bundle hash `index-QTsH0vLD.js` confirmed live at amityx.pages.dev). Verified the fix two ways via `admin.generateLink` (returns the real link Supabase would email, without sending): (1) explicit `redirectTo=https://amityx.pages.dev/signup?verified=1` → action_link's `redirect_to` param decodes to exactly that, allowed; (2) NO redirectTo passed at all (Site-URL fallback, the path a raw API call or the old Login.tsx code would take) → falls back to `https://amityx.pages.dev`, not localhost. Sent the requested real signup to `noel.adv.castillo+amityxsmtp4@gmail.com` (200, confirmation_sent_at set, real unclicked link) for the founder's own click-through verification — deliberately left unconfirmed/undeleted so the link stays valid. No new throwaway cruft left behind (diagnostic `link.check*`/`@amityx.test` accounts self-cleaned by their scripts).

2026-07-18 (later still) · c3-post · orchestrator · fix · B-004 · verified-closed — founder reported "Forgot password isn't working if I provide a wrong password first, then click Forgot password." Reproduced empirically: server-side, the sequence works fine (a raw wrong-password sign-in followed by resetPasswordForEmail returns no error) — ruled out any Supabase-side rate-limit coupling. Drove the actual UI via Playwright against production instead: confirmed the `/auth/v1/recover` request DOES fire correctly and returns no error, but `Login.tsx`'s `forgotPassword()`/`resendVerification()` never cleared the PRIOR sign-in's `error`/`errorKind` state — so the new success notice ("Check your email...") rendered right alongside the stale "email and password don't match" banner. Two contradictory messages on screen read as broken even though the email genuinely sent. Filed as B-004 (S2). Fix: clear error/errorKind at the start of both actions; added a separate `actionBusy` state (distinct from the sign-in button's `busy`) for correct, non-conflicting loading feedback. Added a permanent regression test (`e2e/06-forgot-password-after-failed-login.spec.ts`) reproducing the exact founder sequence. Verified locally against a preview build, committed (`fix/forgot-password-stale-error`, 20f6c4c → PR #11 → merged 356baac), rebuilt, redeployed to Cloudflare Pages (bundle hash confirmed live). Re-ran the regression test against production 3x post-deploy: 3/3 pass (one transient failure immediately after the deploy, before edge propagation settled, not reproducible on retry — attributed to Cloudflare edge-cache lag, not the fix). Verified via `admin.generateLink` (type: recovery) that the reset link's redirect resolves to `https://amityx.pages.dev/reset-password`. Sent one real reset email to `noel.adv.castillo+amityxreset1@gmail.com` (created as a confirmed admin user since resetPasswordForEmail only emails existing accounts) — confirmed dispatched cleanly via auth logs (`action:user_recovery_requested`, `status:200`, no paired error) — left in place, unconfirmed-for-reset, for the founder's click-through. No other throwaway cruft left behind.
