# TR-009: E2E suite + production sign-off (final cycle gate)
Task(s): T-009 (tester half) · Date: 2026-07-13
Verdict: PASS (zero S1/S2 open; B-001 S2 fixed+verified-closed; B-002 S3, B-003 S3 filed — B-003 fixed)

## Levels run
- Smoke (live prod): `npx playwright test e2e/01-smoke.spec.ts` — URL 200, PWA installable, SW registers, deep routes 200, no console errors, no AI/payment hosts contacted.
- E2E/journeys (live prod, Playwright, self-cleaning): `npx playwright test` — 15/15 green.
- Regression: `npm run test:rls` (100/100, grew +2 for B-001) · `npm run lint:vocab` (pass) · local build hash-matches served bundle.
- Security/pen (prod, single-request): `node e2e/adversarial-extra.mjs` — 6 novel probes, 6/6.
- Design review + a11y: axe-core + touch/contrast sweep @375/768/1280, light+dark, screenshots (`app/web/e2e/screens/`).

## Results
- Journey A (owner self-serve → verify → hub → populated /app): GREEN. Account UI no dead-end; provisioning wizard → live hub + class on Today.
- Journey B (CRM sign-in → provision hub → invite owner → owner accepts → /app as OWNER): GREEN. Uncovered + fixed B-001 en route.
- Regression C (anon /h/{slug} booking → owner Requests inbox): GREEN, DB-confirmed, hub-scoped.
- Regression D (guardian /g/{token} → only consented child Mia, no staff/other-hub leak; forged token rejected): GREEN.
- Criterion 3 evidence: live kiosk check-in (1 tap on child tile → "Checked in"). 
- RLS 100/100 incl. new "admin reads hub metadata but NOT tenant data" checks. Adversarial extras: auth.users hidden; guardian tokens unforgeable (6 guesses+injection); anon can't call privileged RPCs; owner can't self-escalate to crm_admin; platform_access_audit append-only (admin can't UPDATE/DELETE); anon blocked from hub_invites + private photo bucket (list+download).
- P.9: vocab pass; NO icon-only buttons anywhere; customer-facing screens ZERO serious/critical axe; 3-tap jobs OK (today 0, check-in 1, post-photo 3 on-screen, read-update 0). Only findings = internal /crm (B-002). Dark mode designed & correct.
- Out-of-scope guards: no AI/LLM code or network calls; no payment SDK/routes (`stripe_customer_id` is an unused reserved type); PWA manifest+SW+icons valid.

## Bugs filed
- B-001 (S2): /crm pipeline showed BLANK hub names/slugs (crm_admin lacked hubs read). FIXED (RLS policy `hubs_crm_read` via migration `20260713150000_crm_hub_read.sql`, applied to prod) → verified-closed (Journey B green, RLS 100/100, tenant data still isolated).
- B-002 (S3, internal /crm): primary-on-muted contrast 4.34:1 + sub-44px desktop links (OPEN, deferred to design skill) + unlabeled status-filter select (FIXED: aria-label, redeployed, axe clean).
- B-003 (S3): "Route stub: / marketing" dev placeholder on prod landing. FIXED (removed) + redeployed + locked with a smoke assertion.

## Evidence
- E2E: 15/15 (`e2e/01..05*.spec.ts`). RLS: `== 100 passed, 0 failed ==`. Adversarial: `== 6 passed, 0 failed ==`. Vocab: 44 files, 0 banned.
- Screenshots: `app/web/e2e/screens/{landing,signup-account,hubpage,app-today,app-roster,app-kiosk,crm-home,crm-hubs,guardian-view}-{375,768,1280}(-dark).png`.
- Live URL: https://amityx.pages.dev (bundle `index-9b_DNz9o.js`, redeployed after fixes; local build == served). Founder protocol: `context/HALLWAY_TEST.md`.
- DB left pristine (11 real hubs, 0 test rows/users).
